/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface ApiPromotionCatalogGet$Params {
  hotelId?: string;
  language?: string;
  search?: string;
  status?: string;
}

export function apiPromotionCatalogGet(http: HttpClient, rootUrl: string, params?: ApiPromotionCatalogGet$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, apiPromotionCatalogGet.PATH, 'get');
  if (params) {
    rb.query('hotelId', params.hotelId, {});
    rb.query('language', params.language, {});
    rb.query('search', params.search, {});
    rb.query('status', params.status, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

apiPromotionCatalogGet.PATH = '/api/promotion-catalog';

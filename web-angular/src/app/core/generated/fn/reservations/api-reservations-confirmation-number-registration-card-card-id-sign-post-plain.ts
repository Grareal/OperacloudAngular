/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RegistrationCard } from '../../models/registration-card';
import { SignRegistrationCardRequest } from '../../models/sign-registration-card-request';

export interface ApiReservationsConfirmationNumberRegistrationCardCardIdSignPost$Plain$Params {
  confirmationNumber: string;
  cardId: string;
      body?: SignRegistrationCardRequest
}

export function apiReservationsConfirmationNumberRegistrationCardCardIdSignPost$Plain(http: HttpClient, rootUrl: string, params: ApiReservationsConfirmationNumberRegistrationCardCardIdSignPost$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<RegistrationCard>> {
  const rb = new RequestBuilder(rootUrl, apiReservationsConfirmationNumberRegistrationCardCardIdSignPost$Plain.PATH, 'post');
  if (params) {
    rb.path('confirmationNumber', params.confirmationNumber, {});
    rb.path('cardId', params.cardId, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RegistrationCard>;
    })
  );
}

apiReservationsConfirmationNumberRegistrationCardCardIdSignPost$Plain.PATH = '/api/reservations/{confirmationNumber}/registration-card/{cardId}/sign';

/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Reservation } from '../../models/reservation';

export interface ApiReservationsSearchGet$Plain$Params {
  term?: string;
  limit?: number;
}

export function apiReservationsSearchGet$Plain(http: HttpClient, rootUrl: string, params?: ApiReservationsSearchGet$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<Reservation>>> {
  const rb = new RequestBuilder(rootUrl, apiReservationsSearchGet$Plain.PATH, 'get');
  if (params) {
    rb.query('term', params.term, {});
    rb.query('limit', params.limit, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<Reservation>>;
    })
  );
}

apiReservationsSearchGet$Plain.PATH = '/api/reservations/search';

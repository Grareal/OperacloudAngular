/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RegistrationCard } from '../../models/registration-card';

export interface ApiRegistrationCardsGeneratePost$Plain$Params {
  confirmationNumber?: string;
  signerEmail?: string;
}

export function apiRegistrationCardsGeneratePost$Plain(http: HttpClient, rootUrl: string, params?: ApiRegistrationCardsGeneratePost$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<RegistrationCard>> {
  const rb = new RequestBuilder(rootUrl, apiRegistrationCardsGeneratePost$Plain.PATH, 'post');
  if (params) {
    rb.query('confirmationNumber', params.confirmationNumber, {});
    rb.query('signerEmail', params.signerEmail, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RegistrationCard>;
    })
  );
}

apiRegistrationCardsGeneratePost$Plain.PATH = '/api/registration-cards/generate';

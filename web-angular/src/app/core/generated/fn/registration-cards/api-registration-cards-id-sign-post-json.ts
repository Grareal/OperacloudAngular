/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RegistrationCard } from '../../models/registration-card';
import { SignRequest } from '../../models/sign-request';

export interface ApiRegistrationCardsIdSignPost$Json$Params {
  id: string;
      body?: SignRequest
}

export function apiRegistrationCardsIdSignPost$Json(http: HttpClient, rootUrl: string, params: ApiRegistrationCardsIdSignPost$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<RegistrationCard>> {
  const rb = new RequestBuilder(rootUrl, apiRegistrationCardsIdSignPost$Json.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RegistrationCard>;
    })
  );
}

apiRegistrationCardsIdSignPost$Json.PATH = '/api/registration-cards/{id}/sign';

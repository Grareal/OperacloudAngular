/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RegistrationCard } from '../../models/registration-card';
import { SignRequest } from '../../models/sign-request';

export interface ApiRegistrationCardsIdSignPost$Plain$Params {
  id: string;
      body?: SignRequest
}

export function apiRegistrationCardsIdSignPost$Plain(http: HttpClient, rootUrl: string, params: ApiRegistrationCardsIdSignPost$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<RegistrationCard>> {
  const rb = new RequestBuilder(rootUrl, apiRegistrationCardsIdSignPost$Plain.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RegistrationCard>;
    })
  );
}

apiRegistrationCardsIdSignPost$Plain.PATH = '/api/registration-cards/{id}/sign';

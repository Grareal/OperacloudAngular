/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RegistrationCard } from '../../models/registration-card';

export interface ApiRegistrationCardsGet$Plain$Params {
  limit?: number;
}

export function apiRegistrationCardsGet$Plain(http: HttpClient, rootUrl: string, params?: ApiRegistrationCardsGet$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<RegistrationCard>>> {
  const rb = new RequestBuilder(rootUrl, apiRegistrationCardsGet$Plain.PATH, 'get');
  if (params) {
    rb.query('limit', params.limit, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<RegistrationCard>>;
    })
  );
}

apiRegistrationCardsGet$Plain.PATH = '/api/registration-cards';
